import { GAMEPLAY_CONFIG } from '../../GameConfig';
import { GameplayConfig } from './ConfigProvider';

/**
 * Test gameplay configuration for automated tests
 * Currently identical to the default config, but can be customized for testing
 */
export const TEST_GAMEPLAY_CONFIG: GameplayConfig = GAMEPLAY_CONFIG;
